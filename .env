# ─── Frontend Local Development ─────────────────────────────
# When running `npm run dev`, the Vite proxy handles /api → localhost:5000
# So no VITE_API_URL is needed for local dev (leave it empty or commented)

VITE_API_URL=
